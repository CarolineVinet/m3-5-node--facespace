<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Facespace</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="header">
      <a href="/"><p class='site-title'>Facespace</p></a>
     <% if(!currentUser.name){ %>
      <a href="/signin" class="signin-button">Sign In</a>
      <% } else { %>
        <a href="/" class="signin-button"><%=`Hello ${currentUser.name}`%></a>
     <% } %>
  </header>
